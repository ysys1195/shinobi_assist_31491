<%= render "shared/second_header"%>
<div class="under_header">
  <div class="error-display">
    <div class="js-message-errors">
      <% if flash[:error_msg] %>
        <br /><%= flash[:error_msg] %>
      <% end %>
    </div>
  </div>
  <div class="copy_invitation_code">
    <div>招待コード</div>
    <div class="copy-box">
      <input name="code" type="text" value=<%= @room.token %> />
    </div>
  </div>
</div>

<% if @room.player_number <= 4 %>

  <div class="outer_status_frame">
    <% @room.player_number.times do |num| %>
      <%= render "char_info", num: num + 1 %>
    <% end %>
  </div>

<% else %>

  <div class="outer_status_frame">
    <% 4.times do |num| %>
      <%= render "char_info", num: num + 1 %>
    <% end %>
  </div>

  <div class="outer_status_frame">
    <% (@room.player_number - 4).times do |num| %>
      <%= render "char_info", num: num + 5 %>
    <% end %>
  </div>

<% end %>